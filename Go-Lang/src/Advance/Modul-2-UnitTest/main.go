package main

// testing.T (struct T) -> digunakan untuk unit test
// testing.M (struct M) -> untuk mengatur life cycle testing (alur eksekusi test unit)
// testing.B (struct B) -> untuk benchmarking (mengukur kecepatan kode program)

