package main

func main(){
	// protocol buffer -> data format that they store structured data which can
	// be serialized or de-serialized by a wide number of different prog.languages. (smaller than xml and json)
}